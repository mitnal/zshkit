setopt prompt_subst

# Set up my prompt.
#autoload promptinit && promptinit && prompt git
#autoload promptinit && promptinit && prompt adam3
#autoload promptinit && promptinit && prompt minimal_vcs
autoload promptinit && promptinit && prompt adam2
#prompt adam3
